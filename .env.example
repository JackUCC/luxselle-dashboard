# ─── Firebase ───
# Local dev: set FIREBASE_USE_EMULATOR=true to use local emulators
# Production: set FIREBASE_USE_EMULATOR=false and provide credentials
FIREBASE_USE_EMULATOR=true
FIREBASE_PROJECT_ID=luxselle-dashboard
FIREBASE_STORAGE_BUCKET=luxselle-dashboard.firebasestorage.app
# Match firebase.json emulator ports (8082, 9198, 4010) to avoid conflicts
FIRESTORE_EMULATOR_HOST=127.0.0.1:8082
FIREBASE_STORAGE_EMULATOR_HOST=127.0.0.1:9198
# Option 1 (Railway/Vercel): Paste entire service account JSON as one line
GOOGLE_APPLICATION_CREDENTIALS_JSON=
# Option 2 (local): File path to service account JSON
GOOGLE_APPLICATION_CREDENTIALS=

# ─── AI ───
# Options: mock | openai | gemini
AI_PROVIDER=mock
OPENAI_API_KEY=
GEMINI_API_KEY=

# ─── GitHub ───
GITHUB_TOKEN=

# ─── App ───
BASE_CURRENCY=EUR
TARGET_MARGIN_PCT=35
PORT=3001

# ─── Supplier Email Sync (Gmail) ───
SUPPLIER_EMAIL_ENABLED=false
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_USER=
SUPPLIER_EMAIL_DEFAULT_QUERY=has:attachment newer_than:30d
SUPPLIER_EMAIL_MAX_ATTACHMENT_MB=10

# ─── Frontend (Vite) ───
VITE_FIREBASE_PROJECT_ID=luxselle-dashboard
VITE_FIREBASE_STORAGE_BUCKET=luxselle-dashboard.firebasestorage.app
# Production: set to your Railway backend URL (no trailing slash)
# VITE_API_BASE=https://your-railway-backend.up.railway.app/api
