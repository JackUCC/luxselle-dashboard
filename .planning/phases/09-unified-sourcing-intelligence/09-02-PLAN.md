---
phase: 09-unified-sourcing-intelligence
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/layout/AnimatedRoutes.tsx
  - src/components/layout/routeMeta.ts
  - src/lib/routePrefetch.ts
  - src/components/widgets/MarketIntelligenceWidget.tsx
  - src/pages/Dashboard/DashboardView.tsx
autonomous: true
requirements:
  - INTEL-01
  - UX-01
must_haves:
  truths:
    - "The unified route is the primary entry in navigation."
    - "Legacy routes /buy-box and /serial-check redirect to /evaluate while preserving search params."
    - "Dashboard and shared widgets route to /evaluate for prefilled searches."
    - "Dashboard activity section has an explicit empty state."
  artifacts:
    - src/components/layout/AnimatedRoutes.tsx
    - src/components/layout/routeMeta.ts
    - src/components/widgets/MarketIntelligenceWidget.tsx
---

<objective>
Wire the new unified route into application navigation, redirects, and prefetch paths while applying targeted design-consistency improvements in high-traffic surfaces.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/layout/AnimatedRoutes.tsx
@src/components/layout/routeMeta.ts
@src/lib/routePrefetch.ts
@src/components/widgets/MarketIntelligenceWidget.tsx
@src/pages/Dashboard/DashboardView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route migration and redirect wiring</name>
  <files>src/components/layout/AnimatedRoutes.tsx</files>
  <action>Register `/evaluate` with the new page and add redirect routes for `/buy-box`, `/serial-check`, and `/evaluator` that preserve current query params.</action>
  <verify>npm run typecheck</verify>
  <done>Legacy routes continue to work and land on `/evaluate` without dropping query state.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation metadata and prefetch updates</name>
  <files>src/components/layout/routeMeta.ts, src/lib/routePrefetch.ts</files>
  <action>Replace separate Price Check + Serial Check primary nav entries with a single unified evaluate route. Update deep-state rule and route prefetch mappings accordingly.</action>
  <verify>npm run typecheck</verify>
  <done>Navigation clearly reflects one unified decision route and prefetch remains valid.</done>
</task>

<task type="auto">
  <name>Task 3: Update entry widgets and dashboard consistency gaps</name>
  <files>src/components/widgets/MarketIntelligenceWidget.tsx, src/pages/Dashboard/DashboardView.tsx</files>
  <action>Point market intelligence prefilled searches to `/evaluate` and add a clear empty state to dashboard activity for consistency with loading/empty conventions.</action>
  <verify>npm run typecheck</verify>
  <done>Prefill flows reach unified page and dashboard activity has consistent empty-state treatment.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck`
- [ ] Manual smoke: dashboard widget search opens `/evaluate?q=...&run=1`
- [ ] Manual smoke: `/buy-box?q=...&run=1` redirects to `/evaluate?q=...&run=1`
</verification>

<success_criteria>
- Unified route is the primary check entry.
- Legacy deep links remain functional.
- High-traffic overview surfaces match loading/empty-state expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/09-unified-sourcing-intelligence/09-02-SUMMARY.md`
</output>
