---
phase: 11-intel-02-agentic-market-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/systemJob.ts
  - packages/shared/src/schemas/index.ts
  - packages/shared/src/schemas/marketIntel.ts
  - packages/server/src/repos/MarketIntelSnapshotRepo.ts
  - packages/server/src/repos/MarketIntelRunRepo.ts
  - packages/server/src/services/market-research/MarketIntelMonitorService.ts
  - packages/server/src/services/JobRunner.ts
  - packages/server/src/routes/market-research.ts
  - packages/server/src/routes/market-research.test.ts
  - packages/server/src/config/env.ts
autonomous: true
requirements: [INTEL-02]
must_haves:
  truths:
    - "Background market-intel refresh can run asynchronously via system jobs and persist snapshot output."
    - "A persisted market-intel snapshot contains generatedAt/freshness metadata for downstream UI usage."
    - "Operator can trigger manual background refresh through market-research route surface."
  artifacts:
    - path: "packages/server/src/services/market-research/MarketIntelMonitorService.ts"
      provides: "Background monitor orchestration and snapshot persistence"
      contains: "runBackgroundRefresh"
    - path: "packages/server/src/repos/MarketIntelSnapshotRepo.ts"
      provides: "Firestore repository for cached market intelligence snapshots"
      contains: "market_intel_snapshots"
    - path: "packages/server/src/routes/market-research.ts"
      provides: "Manual refresh endpoint and snapshot retrieval endpoint"
      contains: "/refresh"
  key_links:
    - from: "JobRunner"
      to: "MarketIntelMonitorService"
      via: "jobType dispatch"
      pattern: "market_intel_refresh"
    - from: "MarketIntelMonitorService"
      to: "MarketIntelSnapshotRepo"
      via: "persistSnapshot"
      pattern: "saveSnapshot"
---

<objective>
Implement the background monitoring foundation for INTEL-02: cache schemas, monitor service, job integration, and trigger/read endpoints.

Purpose: Phase 11 needs durable market-intel cache before deep-dive and UI freshness work can be layered.
Output: Runnable background refresh path with persisted snapshots and route-level access.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-intel-02-agentic-market-intelligence/11-CONTEXT.md
@.planning/phases/11-intel-02-agentic-market-intelligence/11-RESEARCH.md
@packages/server/src/services/JobRunner.ts
@packages/server/src/services/market-research/MarketResearchService.ts
@packages/server/src/routes/market-research.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cache schemas and repos for market intelligence snapshots/runs</name>
  <files>packages/shared/src/schemas/marketIntel.ts, packages/shared/src/schemas/index.ts, packages/server/src/repos/MarketIntelSnapshotRepo.ts, packages/server/src/repos/MarketIntelRunRepo.ts</files>
  <action>Create shared schemas for snapshot and run telemetry records, export them in shared index, and add Firestore repos using BaseRepo for persistence.</action>
  <verify>npm run typecheck</verify>
  <done>Snapshot and run repositories compile and can persist typed records with generatedAt/freshness/cost fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build MarketIntelMonitorService and integrate with JobRunner</name>
  <files>packages/server/src/services/market-research/MarketIntelMonitorService.ts, packages/server/src/services/JobRunner.ts, packages/server/src/config/env.ts</files>
  <action>Implement a monitor service that gathers trending + competitor feed, writes snapshot/run records, and wire a new `market_intel_refresh` job type into JobRunner. Add env guardrails for batch/concurrency/cadence limits.</action>
  <verify>npm test -- packages/server/src/services/market-research/MarketResearchService.test.ts</verify>
  <done>Background job execution path updates system job status and writes a market-intel snapshot on success/failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose refresh and snapshot read endpoints</name>
  <files>packages/server/src/routes/market-research.ts, packages/server/src/routes/market-research.test.ts, packages/shared/src/schemas/systemJob.ts</files>
  <action>Add route endpoints for operator-triggered refresh and latest snapshot retrieval. Reuse jobs route/status conventions and add test coverage for request/response contracts.</action>
  <verify>npm test -- packages/server/src/routes/market-research.test.ts</verify>
  <done>Market-research API exposes background refresh controls and latest snapshot retrieval with passing route tests.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck`
- [ ] `npm test -- packages/server/src/routes/market-research.test.ts`
- [ ] `npm test -- packages/server/src/services/market-research/MarketResearchService.test.ts`
</verification>

<success_criteria>
- Background monitoring can be triggered as a job and persists cache records.
- Snapshot payload contains freshness metadata for UI consumption.
- Route contract for refresh/snapshot is tested and stable.
</success_criteria>

<output>
After completion, create `.planning/phases/11-intel-02-agentic-market-intelligence/11-01-SUMMARY.md`
</output>
