---
phase: 11-intel-02-agentic-market-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/MarketResearch/MarketResearchView.tsx
  - src/pages/MarketResearch/FreshnessBadge.tsx
  - src/components/widgets/AiMarketPulseWidget.tsx
autonomous: true
requirements:
  - INTEL-02

must_haves:
  truths:
    - "When a market research result is displayed, the UI shows the generatedAt age as human-readable text (e.g. '2 minutes ago')"
    - "When a result has mode=background or mode=deep_dive, a mode pill is visible next to the freshness badge"
    - "User can trigger a background monitor refresh from the result panel via a button that calls POST /api/market-research/trigger-monitor"
    - "AiMarketPulseWidget shows a staleness indicator when trending data is older than 1 hour"
  artifacts:
    - path: "src/pages/MarketResearch/MarketResearchView.tsx"
      provides: "Background refresh button, generatedAt age display"
      contains: "trigger-monitor"
    - path: "src/pages/MarketResearch/FreshnessBadge.tsx"
      provides: "Mode pill support"
      contains: "mode"
    - path: "src/components/widgets/AiMarketPulseWidget.tsx"
      provides: "Staleness indicator for trending data"
      contains: "stale"
  key_links:
    - from: "src/pages/MarketResearch/MarketResearchView.tsx"
      to: "/api/market-research/trigger-monitor"
      via: "apiPost call in handleTriggerMonitor"
      pattern: "trigger-monitor"
    - from: "src/pages/MarketResearch/FreshnessBadge.tsx"
      to: "result.intel.mode"
      via: "mode prop rendered as pill"
      pattern: "mode.*pill|pill.*mode"
---

<objective>
Surface market intelligence freshness, mode, and background-refresh controls in the Market Research UI and AiMarketPulseWidget.

Purpose: The CONTEXT success criterion #3 requires "Market Research UI shows live/cached freshness and deep-dive status." The FreshnessBadge exists but the generatedAt age text, mode label, and background-refresh trigger are all missing from the UI. The AiMarketPulseWidget also doesn't surface how old its trending data is.

Output: Updated `MarketResearchView` with generatedAt age text and background-refresh button; updated `FreshnessBadge` with optional mode pill; `AiMarketPulseWidget` with staleness display.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-intel-02-agentic-market-intelligence/11-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/pages/MarketResearch/types.ts:
```typescript
export interface MarketResearchResult {
  // ...
  intel?: {
    runId?: string
    mode?: 'standard' | 'background' | 'deep_dive'
    snapshotAgeMinutes?: number
    freshnessStatus?: 'live' | 'fresh' | 'stale' | 'expired' | 'unknown'
    generatedAt?: string
    cached?: boolean
  }
}
```

From src/pages/MarketResearch/FreshnessBadge.tsx (current):
```typescript
interface FreshnessBadgeProps {
  status?: string   // 'live' | 'fresh' | 'stale' | 'expired' | 'unknown'
  snapshotAgeMinutes?: number
}
// Renders: single colored badge with label like "Live", "Cached · 12m", "Stale cache"
```

From src/pages/MarketResearch/MarketResearchView.tsx (current):
```typescript
// FreshnessBadge is rendered at line 741-744:
<FreshnessBadge
    status={result.intel?.freshnessStatus}
    snapshotAgeMinutes={result.intel?.snapshotAgeMinutes}
/>
// Deep-dive button is at line 768-774 (data-testid="market-research-deep-dive")
// formData has { brand, model, category, condition, colour, year, notes, currentAskPriceEur }
// apiPost is imported from '../../lib/api'
// toast is imported from 'react-hot-toast'
// Loader2 is imported from 'lucide-react'
// isDeepDiveLoading state exists at line 259
```

From src/components/widgets/AiMarketPulseWidget.tsx (current):
```typescript
// Fetches GET /market-research/trending
// State: items, generatedAt (string | null), loading
// generatedAt is already stored in state but NOT displayed
// apiGet imported from '../../lib/api'
```

API contract for background refresh:
```
POST /api/market-research/trigger-monitor
Body: { brand, model, category, condition, colour?, year?, notes?, currentAskPriceEur? }
Response 202: { data: { jobId: string, status: 'queued' } }
```

Formatter available:
```typescript
// src/lib/formatters.ts
export const formatRelativeDate = (dateStr: string) => { ... }
// Returns strings like "2 minutes ago", "1 hour ago", "3 days ago"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update FreshnessBadge to show mode pill and update MarketResearchView with age text and background-refresh button</name>
  <files>
    src/pages/MarketResearch/FreshnessBadge.tsx
    src/pages/MarketResearch/MarketResearchView.tsx
  </files>
  <action>
**FreshnessBadge.tsx — add optional `mode` and `generatedAt` props:**

Update `FreshnessBadgeProps` to include:
```typescript
interface FreshnessBadgeProps {
  status?: string
  snapshotAgeMinutes?: number
  mode?: 'standard' | 'background' | 'deep_dive'
  generatedAt?: string
}
```

After the existing badge span, conditionally render a mode pill when `mode` is `'background'` or `'deep_dive'`:
```typescript
// MODE_LABELS map:
const MODE_LABELS: Record<string, string> = {
  background: 'Background run',
  deep_dive: 'Deep dive',
}
```

Render the mode pill only when `mode` is `'background'` or `'deep_dive'` (not for `'standard'` or undefined). Style: `bg-lux-100 text-lux-600 text-[11px] font-semibold rounded-full px-2 py-0.5`.

Wrap both badge and pill in a `flex items-center gap-1.5` container.

Also render `generatedAt` age text below (or beside) the badges if `generatedAt` is provided:
```typescript
// Use formatRelativeDate from '../../lib/formatters' (import it)
// Render: <span className="text-[10px] text-lux-400">{formatRelativeDate(generatedAt)}</span>
// Only render if generatedAt is a valid non-empty string
```

**MarketResearchView.tsx changes:**

1. Pass `mode` and `generatedAt` to `FreshnessBadge` (at the existing usage around line 741-744):
```tsx
<FreshnessBadge
    status={result.intel?.freshnessStatus}
    snapshotAgeMinutes={result.intel?.snapshotAgeMinutes}
    mode={result.intel?.mode}
    generatedAt={result.intel?.generatedAt}
/>
```

2. Add `isMonitorLoading` state:
```typescript
const [isMonitorLoading, setIsMonitorLoading] = useState(false)
```

3. Add `handleTriggerMonitor` function near `handleDeepDive`:
```typescript
const handleTriggerMonitor = async () => {
    if (!formData.brand || !formData.model) return
    setIsMonitorLoading(true)
    try {
        await apiPost('/market-research/trigger-monitor', {
            ...formData,
            currentAskPriceEur: formData.currentAskPriceEur ? Number(formData.currentAskPriceEur) : undefined,
        })
        toast.success('Background refresh queued')
    } catch {
        toast.error('Failed to queue background refresh')
    } finally {
        setIsMonitorLoading(false)
    }
}
```

4. Add a "Refresh" button in the `headerActions` div, alongside the existing "Deep-dive" and "Save" buttons (insert it after the deep-dive button):
```tsx
<button
    type="button"
    onClick={handleTriggerMonitor}
    disabled={isMonitorLoading}
    className="inline-flex min-h-[44px] items-center gap-1.5 rounded-lg border border-lux-200 bg-white px-3 py-1.5 text-sm font-medium text-lux-700 hover:bg-lux-50 disabled:cursor-not-allowed disabled:opacity-60"
    data-testid="market-research-trigger-monitor"
    title="Queue a background intelligence refresh for this item"
>
    {isMonitorLoading ? <Loader2 className="h-4 w-4 animate-spin" /> : <History className="h-4 w-4" />}
    Refresh
</button>
```

Note: `History` icon is already imported in `MarketResearchView.tsx` (line ~17). `Loader2` is also already imported.

Do NOT change any existing functionality or remove any existing elements. All existing state, handlers, and rendering must remain intact.

Run `npm run typecheck` to verify no type errors.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    `FreshnessBadge` accepts `mode` and `generatedAt` props and renders a mode pill for background/deep_dive runs plus age text. `MarketResearchView` shows the updated badge and has a "Refresh" button that calls `/market-research/trigger-monitor`. `npm run typecheck` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add staleness indicator to AiMarketPulseWidget</name>
  <files>src/components/widgets/AiMarketPulseWidget.tsx</files>
  <action>
`AiMarketPulseWidget` already fetches `GET /market-research/trending` and stores `generatedAt` in state. The widget currently does NOT display how old the trending data is.

Add a staleness indicator in the widget footer area (below the items list):

1. Compute whether data is stale: `generatedAt` older than 60 minutes = stale.

```typescript
function isStaleData(generatedAt: string | null): boolean {
  if (!generatedAt) return false
  const ageMs = Date.now() - new Date(generatedAt).getTime()
  return ageMs > 60 * 60 * 1000  // 60 minutes
}
```

2. When `generatedAt` is set, display a small age line at the bottom of the widget content area:

```tsx
{generatedAt && (
  <p className="mt-3 text-[11px] text-lux-400">
    {isStaleData(generatedAt) ? (
      <span className="text-amber-500">Data may be outdated · {formatRelativeDate(generatedAt)}</span>
    ) : (
      <span>Updated {formatRelativeDate(generatedAt)}</span>
    )}
  </p>
)}
```

Import `formatRelativeDate` from `'../../lib/formatters'`.

This is a purely additive change. Do NOT modify the fetching logic, loading state, or item rendering. Do NOT change the widget's title or header.

Run `npm run typecheck` after the change.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    `AiMarketPulseWidget` displays "Updated X minutes ago" when data is fresh, "Data may be outdated · X hours ago" in amber when stale (>60 min old). `npm run typecheck` passes.
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes with 0 errors
- `npm test` passes (all existing tests still green — these are UI-only changes, no route changes)
- `FreshnessBadge` renders mode pill for `mode='background'` and `mode='deep_dive'` (not for `mode='standard'`)
- `FreshnessBadge` renders `formatRelativeDate(generatedAt)` age text when `generatedAt` is provided
- `MarketResearchView` has `data-testid="market-research-trigger-monitor"` button
- `AiMarketPulseWidget` displays updated-at timestamp with amber warning when stale
</verification>

<success_criteria>
- After a deep-dive run, the AI Analysis card shows both the freshness badge AND a "Deep dive" mode pill AND the generatedAt age text
- User can click "Refresh" button to queue a background refresh for the current item
- AiMarketPulseWidget footer shows when trending data was last generated, with amber color when over 1 hour old
- No TypeScript errors, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/11-intel-02-agentic-market-intelligence/11-02-SUMMARY.md`
</output>
