# Railway deployment config (backend only). Frontend deploys to Vercel.
# See docs/deploy/RAILWAY.md for env vars and setup.

[build]
builder = "nixpacks"
buildCommand = "npm install --omit=dev"

[deploy]
startCommand = "npm run start --workspace=@luxselle/server"
