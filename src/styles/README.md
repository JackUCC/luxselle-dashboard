# Styles

Global frontend styling.

- **index.css** — Tailwind directives, CSS variables (fonts, colors), and shared utility classes (e.g. `.lux-card`, `.lux-input`, `.lux-btn-primary`). Entry point: imported from `main.tsx`.

Tailwind config is at repo root: `tailwind.config.js`. PostCSS: `postcss.config.js`.
